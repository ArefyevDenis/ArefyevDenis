#include "factory.h"

//#include <sstream>

#include <QObject>

namespace FACT {

////  std::map<std::string,CCreator<QObject>*> Factory::map_crt;

////  Factory::Factory()
////  { who(); }

////  void Factory::rgg(std::string key ,CCreator<QObject>* fn)
////  {
////    map_crt[key] = fn;
////    LOG(key);
////  }

////   QObject* Factory::crt(QObject* parent,std::string key)
////  {
////     return map_crt[key]->Create(parent);
////  }

//  std::map<std::string,CCreator<QObject,QObject>*> Factory<QObject,QObject>::map_crt;

}



